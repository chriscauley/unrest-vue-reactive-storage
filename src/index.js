import LocalStorage, { ReactiveLocalStorage } from './LocalStorage'
import MemoryStorage from './MemoryStorage'
import RestStorage, { ReactiveRestApi, getClient } from './RestStorage'

export default {
  LocalStorage,
  ReactiveLocalStorage,
  MemoryStorage,
  RestStorage,
  ReactiveRestApi,
  getClient,
}
export {
  LocalStorage,
  ReactiveLocalStorage,
  MemoryStorage,
  RestStorage,
  ReactiveRestApi,
  getClient,
}
